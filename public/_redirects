# Widget standalone page (must come BEFORE the SPA catch-all)
/widget        /widget.html   200
/widget.html   /widget.html   200
# Main app SPA fallback. On the TradeLane site, the build step replaces
# index.html with the TradeLane page (via SITE_TARGET=tradelane), so this
# correctly serves TradeLane there and the Vantage app everywhere else.
/*             /index.html    200
